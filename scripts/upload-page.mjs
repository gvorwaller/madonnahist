#!/usr/bin/env node
// scripts/upload-page.mjs
// Stub — real implementation is td-ccb503 (Phase 1).
// Will: validate filename, upload original to DO Spaces, slice into day cells,
// insert calendar_pages + 28-31 calendar_days rows, enqueue OCR jobs.

console.error('upload-page.mjs: not yet implemented (td-ccb503). Path reserved.');
process.exit(2);
