# 2026-06-01 Devlog

## 13:11 EST - CODEX Review Integration + Adversarial Review Pass + Commit

### Summary
Incorporated CODEX's critical review feedback into the capture intake pipeline plan, ran a single-pass adversarial code review that found 3 P1 / 5 P2 / 5 P3 issues, fixed all of them, and committed the full pipeline (16 files, 3,315 lines).

### What Was Done
- **CODEX critical review** sent and received via claude-relay. 4 P1 and 4 P2 findings. All addressed in the plan doc (`docs/2026-05-29-capture-intake-pipeline.md`):
  - Added explicit capture-station deployment model (local SvelteKit during shoots)
  - Switched from brittle header-strip crops to full-page Vision pass with bounding-box location
  - Hardened `capture_intake` table: `content_hash`, `file_size_bytes`, `file_mtime`, `supersedes_id`, `ingest_phase/attempts/error`
  - Merged approve+ingest into single action (no hidden limbo state)
  - Added grid layout selector to block ingestion for unrecognized layouts
  - Path security: all actions on `capture_intake.id`, symlink rejection
- **Adversarial code review** (single Claude pass, no Codex cross-review):
  - P1: Preview endpoint was serving full ~15MB camera JPEGs ignoring the `w` param — now resizes + EXIF-normalizes
  - P1: Ingest batch loop had no try/catch — one throw killed the entire batch
  - P1: `Number('')` produced 0 when dropdown reset to placeholder — now passes `undefined`
  - P2: `ON CONFLICT DO NOTHING` was too broad — narrowed to `(tether_session, camera_filename)`
  - P2: `withTransaction` ROLLBACK failure masked the original error
  - P2: Ingest button showed total checked count instead of valid-item count
  - P2: Preview `maxDim` had no range validation
  - P2: No status guard before ingesting (rejected/reshoot rows could be re-ingested)
  - P3: `exposureOf()` decoded the analysis buffer twice — consolidated to single decode
  - P3: Removed unreachable `'approved'` from DB CHECK constraint
  - P3: Classify + quality now run sequentially (halves peak memory per image)
  - P3: Preview cache reduced from 1hr to 60s
- **Committed** as `6bf6861` — `npm run check` clean (0 errors, 0 warnings)

### Files Modified
- `docs/2026-05-29-capture-intake-pipeline.md` (CODEX review integration)
- `backend/db/migrations/0007_capture_intake.sql` (removed unreachable `approved` status)
- `src/lib/db.ts` (`withTransaction` ROLLBACK safety)
- `src/lib/ingest/quality.ts` (single-decode `exposureOf`)
- `src/routes/admin/capture/+page.server.ts` (try/catch in ingest loop, status guard, sequential classify+quality, narrowed ON CONFLICT)
- `src/routes/admin/capture/+page.svelte` (dropdown undefined handling, button count fix)
- `src/routes/admin/capture/preview/[...path]/+server.ts` (resize thumbnails, maxDim clamp, short cache)

### Key Technical Insight
The adversarial review caught a real UX trap: `Number('')` silently produces `0`, which is a valid number but matches no dropdown option. The select gets stuck in an unrecoverable state requiring a page reload. Any `<select>` with a placeholder `<option value="">` that feeds into `Number()` has this bug — always guard for the empty string before coercing.

The CODEX review's most valuable finding was the deployment model gap — the plan assumed the server could read `~/Lumix Tether` but production runs on a remote droplet. Making the capture-station model explicit early avoided a class of "works in dev, breaks in prod" issues.

### Next Steps
- Run the app and test `/admin/capture` UI end-to-end with real tether images
- Calibrate quality thresholds against true G9 II + 60mm rig captures
- E2E at capture station: discover -> classify -> ingest -> verify DB rows + Spaces objects
- OCR worker to consume `job_runs` and crop day cells from page images
